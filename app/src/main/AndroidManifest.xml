<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icono_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icono_app"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListaProcedimientosActivity"
            android:label="LISTA PROCEDIMIENTOS">

        </activity>
        <activity
            android:name=".RegistroUsuariosActivity"
            android:label="REGISTRO DE USUARIOS">

        </activity>
        <activity
            android:name=".RegistroActivity"
            android:label="REGISTRO">

        </activity>
        <activity
            android:name=".HomeActivity"
            android:label="HOME" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>